pipeline {
    agent any

    stages{
        stage("ansible-config"){
            steps{
                script {
                    sh '''
                    ls -ltr 
                    pwd
                    '''
                }
            }
        }
    }
}
