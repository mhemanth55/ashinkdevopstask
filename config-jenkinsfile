pipeline {
    agent any

    stages{
        stage("ansible-config"){
            steps{
                script {
                    sh '''
                    ls -ltr 
                    pwd
                    cd ansible
                    sudo chmod 666 DevOps-Key.pem
                    sudo ansible -i inventory  -m ping all
                    '''
                }
            }
        }
    }
}
