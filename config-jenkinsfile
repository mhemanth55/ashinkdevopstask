pipeline {
    agent any

    stages{
        stage("ansible-config"){
            steps{
                script {
                    sh '''
                    ls -ltr 
                    pwd
                    cd ansible
                    ansible -i inventory  -m ping all
                    '''
                }
            }
        }
    }
}
