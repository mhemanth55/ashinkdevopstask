pipeline {
    agent any

    stages{
        stage("ansible-config"){
            steps{
                script {
                    sh '''
                    ls -ltr 
                    pwd
                    cd ansible
                    chmod 666 DevOps-Key.pem
                    ansible -i inventory  -m ping all
                    '''
                }
            }
        }
    }
}
